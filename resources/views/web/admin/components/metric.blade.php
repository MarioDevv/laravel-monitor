@props(['title', 'value'])

<div class="flex-1 text-center">
    <h5 class="text-xl font-semibold text-slate-800">{{ $value }}</h5>
    <p class="text-sm font-light text-slate-600">{{ $title }}</p>
</div>
